var firstone = getElementbyid("phrase")
firstlength = firstone.length;
// this length will be added to length
function Palindrome(Firstone) {
 
        for (let index = 0; index < first.length; index++) {
            
            // this loop will run till the end of phrase 
            // if  there is empty space in words this word will be send to another function to check Palindrome
            // if it is Palindrome then it will write on html and count Palindrome will be increase by one 
            
        }


    
     
  
}
 